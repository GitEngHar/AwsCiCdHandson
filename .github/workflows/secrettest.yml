name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          NAME_SECRET: ${{secrets.SECRET_TEST_MYNAME}} 
        run: |
          echo "$NAME_SECRET"
